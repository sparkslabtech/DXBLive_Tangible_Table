using RenderHeads.Media.AVProVideo;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Video;

public class TempTitleVideos : MonoBehaviour
{

    public MediaPlayer mediaPlayerTangible;
    public VideoPathLoader videoPathLoader;
    public List<VideoClip> idleClipList;
    public SideTangibleHandler sideTangible;

    // Start is called before the first frame update
    void Start()
    {
        //sideTangible.
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void AssignVideo()
    {
        //mediaPlayerTangible.Play();
    }
}
